import React from "react";
import { Paper, Button } from "@mui/material";

function Item({ item }) {
  return (
    <Paper>
      <Button className="CheckButton">
      </Button>
    </Paper>
  );
}

export default Item;
